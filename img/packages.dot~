digraph "Packages" {
	label = "Interface Pattern";
	rankdir=LR;
	
	node[shape=box];
	edge[arrowhead=onormal];

	IFrameDataSource;
	ISimDataSource
	ISliceDataSource;

	SstDataSource ->IFrameDataSource;
	SstDataSource ->ISimDataSource;	

	NavSliceDataSource->IFrameDataSource[label="uses",arrowhead=vee,style=dashed];
	

	data;
	inter

	nav->inter;
	nav->data[arrowhead=vee];

	alg->inter;
	alg->data;

	tiling->inter;
	tiling->data[arrowhead=vee];
	
	sst->inter;
	sst->data;
	

	toy->nav;
	toy->data;
	toy->tiling;
	toy->sst;
	

	}